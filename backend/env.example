# RAG Chatbot Backend - Environment Configuration
# Retrieval-Augmented Generation chatbot with document upload and question answering

# =============================================================================
# CORE SETTINGS
# =============================================================================
ENV=dev                 # dev | prod
PORT=8080
LOG_LEVEL=INFO          # DEBUG | INFO | WARNING | ERROR
LOG_FORMAT=json         # json | text

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================
# Development: Files saved to local directory
LOCAL_BUCKET_DIR=/tmp/rag-documents

# Production: GCS bucket for file storage (requires workload identity or service account)
GCS_BUCKET=your-production-bucket-name

# =============================================================================
# LLM INTEGRATION (OpenAI)
# =============================================================================
# Required for RAG question answering
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=your-openai-api-key-here

# Model Configuration
LLM_DEFAULT_MODEL=gpt-3.5-turbo
LLM_DEFAULT_PROVIDER=openai
LLM_DEFAULT_TEMPERATURE=0.1         # Low temperature for consistent answers
LLM_DEFAULT_MAX_TOKENS=2000

# =============================================================================
# VECTOR DATABASE CONFIGURATION
# =============================================================================
# ChromaDB settings
VECTOR_DB_PATH=/tmp/rag-documents/chroma_db

# =============================================================================
# APPLICATION METADATA
# =============================================================================
APP_NAME=rag-chatbot
APP_VERSION=1.0.0
